using UnityEngine;

public class Collection : MonoBehaviour
{

    public int[] HowManyCards = new int[1000];
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
